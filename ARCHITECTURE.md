# 🏗️ System Architecture Diagrams

Visual representations of the AI Assessment Platform architecture.

---

## 📊 High-Level System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         Users                                    │
│                                                                  │
│         Recruiters                    Candidates                │
│         (Create Jobs)                 (Take Assessments)        │
└─────────────┬───────────────────────────┬──────────────────────┘
              │                           │
              │         Browser           │
              │                           │
┌─────────────┴───────────────────────────┴──────────────────────┐
│                    Frontend (React)                             │
│                                                                  │
│  • Recruiter Dashboard     • Assessment Interface               │
│  • Candidate Dashboard     • Results Display                    │
│  • Job Management          • Leaderboard View                   │
└──────────────────────────────┬───────────────────────────────┬─┘
                               │                                 │
                         REST API                           WebSocket
                               │                           (Future)
┌──────────────────────────────┴──────────────────────────────┐
│                   Backend (FastAPI)                         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │           API Route Controllers                      │  │
│  │  • Auth   • Jobs   • Assessments   • Results        │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌───────────┐  ┌────────────┐  ┌──────────┐  ┌────────┐ │
│  │  Gemini   │  │    Code    │  │Plagiarism│  │Anomaly │ │
│  │  Service  │  │  Executor  │  │ Detector │  │Detector│ │
│  └───────────┘  └────────────┘  └──────────┘  └────────┘ │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │         Database Layer (SQLAlchemy ORM)              │  │
│  └─────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────┴──────────────────────────────┐
│               External Services & Storage                │
│                                                          │
│  ┌───────────────┐            ┌─────────────────┐      │
│  │  Gemini API   │            │   Database      │      │
│  │  (Google AI)  │            │   (SQLite)      │      │
│  └───────────────┘            └─────────────────┘      │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Job Creation Flow

```
Recruiter
   │
   │ 1. Submit Job Description
   ▼
┌──────────────────┐
│  Frontend        │
│  (React)         │
└────────┬─────────┘
         │ 2. POST /jobs (JD text)
         ▼
┌──────────────────┐
│  Backend API     │
│  (FastAPI)       │
└────────┬─────────┘
         │ 3. Parse JD
         ▼
┌──────────────────┐
│  Gemini Service  │
│  - Extract skills│  ← → Gemini API
│  - Get exp level │
│  - Identify role │
└────────┬─────────┘
         │ 4. Parsed data
         ▼
┌──────────────────┐
│  Backend API     │
│  - Generate Qs   │
└────────┬─────────┘
         │ 5. Create questions
         ▼
┌──────────────────┐
│  Gemini Service  │
│  - Gen MCQs      │  ← → Gemini API
│  - Gen Subjective│
│  - Gen Coding    │
└────────┬─────────┘
         │ 6. Questions list
         ▼
┌──────────────────┐
│  Database        │
│  - Save Job      │
│  - Save Questions│
└────────┬─────────┘
         │ 7. Success response
         ▼
      Frontend
         │
         ▼
     Recruiter sees created job
```

---

### 2. Assessment Taking Flow

```
Candidate
   │
   │ 1. Start Assessment
   ▼
┌──────────────────┐
│  Frontend        │
│  POST /assessment│
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Backend         │
│  Create record   │
└────────┬─────────┘
         │ 2. Fetch questions
         ▼
┌──────────────────┐
│  Database        │
│  Return questions│
└────────┬─────────┘
         │
         ▼
      Frontend
         │ 3. Display question
         ▼
     Candidate
         │ 4. Submit answer
         ▼
      Frontend
         │ 5. POST /submit
         ▼
┌──────────────────┐
│  Backend         │
│  Route answer to │
│  correct handler │
└────────┬─────────┘
         │
         ▼
   ┌────────────────────────────┐
   │    Question Type?          │
   └──┬────────┬────────┬───────┘
      │        │        │
   MCQ│   Subj.│   Code│
      │        │        │
      ▼        ▼        ▼
   ┌────┐  ┌────┐   ┌────┐
   │Auto│  │ AI │   │Exec│
   │Eval│  │Eval│   │Test│
   └──┬─┘  └──┬─┘   └──┬─┘
      │       │        │
      │       │  Gemini│
      │       └────────┤
      │                │
      └────────┬───────┘
               │ 6. Score
               ▼
         ┌──────────┐
         │Plagiarism│
         │  Check   │
         └────┬─────┘
              │ 7. Final score
              ▼
         ┌──────────┐
         │ Database │
         │  Save    │
         └────┬─────┘
              │
              ▼
           Frontend
              │
              ▼
        Show feedback
              │
              ▼
          Candidate
```

---

### 3. Evaluation & Ranking Flow

```
Complete Assessment
        │
        │ 1. Complete request
        ▼
┌──────────────────┐
│  Backend         │
│  Calculate total │
└────────┬─────────┘
         │ 2. Get all submissions
         ▼
┌──────────────────┐
│  Database        │
│  Return records  │
└────────┬─────────┘
         │ 3. Aggregate scores
         ▼
┌──────────────────┐
│  Backend         │
│  Score by:       │
│  - Type (MCQ etc)│
│  - Skill         │
└────────┬─────────┘
         │ 4. Detect anomalies
         ▼
┌──────────────────┐
│ Anomaly Detector │
│ - Fast submit?   │
│ - Random pattern?│
│ - High plagiarism│
└────────┬─────────┘
         │ 5. Generate report
         ▼
┌──────────────────┐
│  Gemini Service  │  ← → Gemini API
│  - Strengths     │
│  - Weaknesses    │
│  - Skill gaps    │
│  - Recommendation│
└────────┬─────────┘
         │ 6. Calculate ranking
         ▼
┌──────────────────┐
│  Backend         │
│  Sort by score   │
│  Assign ranks    │
└────────┬─────────┘
         │ 7. Save evaluation
         ▼
┌──────────────────┐
│  Database        │
│  Store complete  │
│  report          │
└────────┬─────────┘
         │ 8. Return results
         ▼
      Frontend
         │
         ▼
    Display results
```

---

## 🗄️ Database Schema

```
┌─────────────────────┐
│       Users         │
├─────────────────────┤
│ id (PK)             │
│ email               │
│ hashed_password     │
│ full_name           │
│ role                │◄───────┐
│ created_at          │        │
└──────┬──────────────┘        │
       │                       │
       │ 1:N                   │ N:1
       │                       │
┌──────▼──────────────┐   ┌────┴───────────────┐
│       Jobs          │   │   Assessments      │
├─────────────────────┤   ├────────────────────┤
│ id (PK)             │   │ id (PK)            │
│ title               │   │ job_id (FK)        │
│ description         │   │ candidate_id (FK)  │
│ recruiter_id (FK)   │   │ status             │
│ required_skills     │   │ total_score        │
│ experience_level    │   │ percentage         │
│ duration_minutes    │   │ rank               │
│ cutoff_percentage   │   │ is_suspicious      │
│ created_at          │   │ completed_at       │
└──────┬──────────────┘   └─────┬──────────────┘
       │                        │
       │ 1:N                    │ 1:N
       │                        │
┌──────▼──────────────┐   ┌─────▼──────────────┐
│     Questions       │   │   Submissions      │
├─────────────────────┤   ├────────────────────┤
│ id (PK)             │   │ id (PK)            │
│ job_id (FK)         │   │ assessment_id (FK) │
│ question_type       │   │ question_id (FK)   │
│ question_text       │   │ answer             │
│ difficulty          │   │ selected_option    │
│ skill_tested        │   │ code_submission    │
│ options             │   │ score              │
│ correct_answer      │   │ is_correct         │
│ test_cases          │   │ ai_feedback        │
│ max_score           │   │ plagiarism_score   │
└─────────────────────┘   └────────────────────┘
                                   │
                                   │ N:1
                                   │
                          ┌────────▼───────────┐
                          │   Evaluations      │
                          ├────────────────────┤
                          │ id (PK)            │
                          │ assessment_id (FK) │
                          │ strengths          │
                          │ weaknesses         │
                          │ skill_gaps         │
                          │ skill_scores       │
                          │ mcq_score          │
                          │ subjective_score   │
                          │ coding_score       │
                          │ percentile         │
                          │ qualified          │
                          │ ai_summary         │
                          │ recommendation     │
                          └────────────────────┘
```

---

## 🧩 Component Architecture

### Frontend Components

```
App.jsx (Root)
│
├─ Header.jsx
│  └─ Navigation, User info, Logout
│
├─ Routes
│  │
│  ├─ Login.jsx
│  │  └─ Form, Validation
│  │
│  ├─ Register.jsx
│  │  └─ Form, Role selection
│  │
│  ├─ RecruiterDashboard.jsx
│  │  ├─ Job list
│  │  ├─ Create job form
│  │  └─ Job cards
│  │
│  ├─ CandidateDashboard.jsx
│  │  ├─ Assessment list
│  │  └─ Assessment cards
│  │
│  ├─ Assessment.jsx
│  │  ├─ Progress bar
│  │  ├─ Question navigation
│  │  ├─ Question display
│  │  │  ├─ MCQ component
│  │  │  ├─ Subjective textarea
│  │  │  └─ Code editor
│  │  └─ Navigation buttons
│  │
│  ├─ Results.jsx
│  │  ├─ Overall score
│  │  ├─ Section scores
│  │  ├─ Skill breakdown
│  │  ├─ Strengths/Weaknesses
│  │  └─ AI summary
│  │
│  └─ Leaderboard.jsx
│     ├─ Ranking table
│     └─ Top performers
│
└─ api.js
   └─ API integration
```

### Backend Modules

```
main.py (FastAPI App)
│
├─ Route Handlers
│  ├─ /register
│  ├─ /token
│  ├─ /jobs
│  ├─ /assessments
│  ├─ /submit
│  └─ /results
│
├─ Dependencies
│  ├─ get_db()
│  └─ get_current_user()
│
└─ Services
   │
   ├─ gemini_service.py
   │  ├─ parse_job_description()
   │  ├─ generate_questions()
   │  ├─ evaluate_subjective_answer()
   │  ├─ generate_evaluation_report()
   │  └─ detect_resume_mismatch()
   │
   ├─ assessment_utils.py
   │  ├─ CodeExecutor
   │  │  └─ execute_python_code()
   │  ├─ PlagiarismDetector
   │  │  ├─ check_similarity()
   │  │  └─ check_against_database()
   │  └─ AnomalyDetector
   │     └─ detect_anomalies()
   │
   ├─ auth.py
   │  ├─ verify_password()
   │  ├─ get_password_hash()
   │  ├─ create_access_token()
   │  └─ verify_token()
   │
   ├─ database.py
   │  ├─ engine
   │  ├─ SessionLocal
   │  └─ get_db()
   │
   ├─ models.py
   │  ├─ User
   │  ├─ Job
   │  ├─ Question
   │  ├─ Assessment
   │  ├─ Submission
   │  └─ Evaluation
   │
   ├─ schemas.py
   │  └─ Pydantic models
   │
   └─ config.py
      └─ Settings
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│           User Request                  │
└───────────────┬─────────────────────────┘
                │
                ▼
    ┌───────────────────────┐
    │   HTTPS/SSL (Prod)    │
    │   Encrypted Transport │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │   CORS Middleware     │
    │   Check origin        │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  JWT Validation       │
    │  Verify token         │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  Role Check           │
    │  Verify permissions   │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  Input Validation     │
    │  Pydantic schemas     │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  SQL Injection Protect│
    │  ORM parameterization │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  Business Logic       │
    │  Process request      │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  Response             │
    │  Send to client       │
    └───────────────────────┘
```

---

## 📈 Scalability Architecture

```
Current (Single Instance):
┌──────────┐     ┌──────────┐
│ Frontend │────▶│ Backend  │
│  (Vite)  │     │(FastAPI) │
└──────────┘     └─────┬────┘
                       │
                       ▼
                 ┌──────────┐
                 │ Database │
                 │(SQLite)  │
                 └──────────┘

Future (Scaled):
┌──────────┐     ┌────────────────┐
│ Frontend │────▶│ Load Balancer  │
│  (CDN)   │     └───────┬────────┘
└──────────┘             │
                ┌────────┴────────┐
                │                 │
          ┌─────▼─────┐   ┌──────▼────┐
          │ Backend 1 │   │ Backend 2 │
          │ (FastAPI) │   │ (FastAPI) │
          └─────┬─────┘   └──────┬────┘
                │                 │
                └────────┬────────┘
                         │
                   ┌─────▼──────┐
                   │  Database  │
                   │(PostgreSQL)│
                   │  Cluster   │
                   └────────────┘
```

---

## 🧪 Testing Architecture

```
┌──────────────────────────────────┐
│        Manual Testing            │
│  • UI testing via browser        │
│  • API testing via /docs         │
└──────────────┬───────────────────┘
               │
               ▼
┌──────────────────────────────────┐
│      Integration Testing         │
│  • End-to-end user flows         │
│  • API endpoint testing          │
└──────────────┬───────────────────┘
               │
               ▼
┌──────────────────────────────────┐
│       Unit Testing (Future)      │
│  • Function-level tests          │
│  • Component tests               │
└──────────────────────────────────┘
```

---

## 🔄 Deployment Architecture

```
Development:
┌──────────────────┐
│  Local Machine   │
│  ├─ Backend      │
│  │  localhost:8000
│  └─ Frontend     │
│     localhost:5173
└──────────────────┘

Production:
┌──────────────────────────────────┐
│         Cloud Provider           │
│  ┌────────────┐  ┌────────────┐ │
│  │  Frontend  │  │  Backend   │ │
│  │   (CDN)    │  │  (Server)  │ │
│  │ domain.com │  │ api.domain │ │
│  └────────────┘  └─────┬──────┘ │
│                        │         │
│                  ┌─────▼──────┐  │
│                  │  Database  │  │
│                  │(PostgreSQL)│  │
│                  └────────────┘  │
└──────────────────────────────────┘
```

---

These diagrams provide visual representations of how the AI Assessment Platform is architected and how data flows through the system. Use them for understanding, documentation, and presentations.
